# Supabase
# Estas credenciales se copian automáticamente desde CyberCita usando: npm run copy-credentials
# O puedes copiarlas manualmente desde cybercitas/.env cambiando:
#   VITE_SUPABASE_URL -> NEXT_PUBLIC_SUPABASE_URL
#   VITE_SUPABASE_ANON_KEY -> NEXT_PUBLIC_SUPABASE_ANON_KEY
#   VITE_VAPID_PUBLIC_KEY -> NEXT_PUBLIC_VAPID_PUBLIC_KEY

NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# VAPID Keys (para Web Push Notifications)
# Generar con: node scripts/generate-vapid-keys.js
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# PWA Configuration
NEXT_PUBLIC_ENABLE_PWA=true
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# VAPID Keys (para Web Push Notifications)
# Generar con: node scripts/generate-vapid-keys.js
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key

# PWA Configuration
NEXT_PUBLIC_ENABLE_PWA=true
NEXT_PUBLIC_SITE_URL=http://localhost:3000
